import '../repository/movie_repository.dart';

class SaveFavoriteMovie {
  // final MoviesRepository moviesRepository;
  //
  // SaveFavoriteMovie(this.moviesRepository);
  //
  // Future<bool> saveToFavoritesList(int id) {
  //   return moviesRepository.saveToFavourites(id);
  // }
}